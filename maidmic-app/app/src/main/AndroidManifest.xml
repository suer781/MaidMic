<?xml version="1.0" encoding="utf-8"?>
<!--
  MaidMic AndroidManifest.xml
  ============================================================
  声明所有的 Activity、Service、权限。
  
  架构路线：三路桥接（Shizuku/无障碍）+ 双设备流式传输
  
  - Shizuku: 无需Root，通过Shizuku API拦截AudioRecord
  - 无障碍: 通过AccessibilityService桥接
  - 流式传输: 两台设备共存，A采集DSP→WiFi→B接收注入系统
  
  权限说明：
    - RECORD_AUDIO: 必须，用于捕获麦克风输入
    - FOREGROUND_SERVICE: 必须，后台音频处理需要前台通知
    - BIND_ACCESSIBILITY_SERVICE: 方案C需要
    - REQUEST_IGNORE_BATTERY_OPTIMIZATIONS: 引导页"后台保活"（电池优化白名单）
    - 其他 Shizuku 相关权限
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================================
         权限声明
         ============================================================ -->

    <!-- 录音权限（麦克风输入） — 核心权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- WiFi / 网络权限（双设备流式传输） -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <!-- 蓝牙权限（流式传输） -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="28" />

    <!-- 前台服务（Android 8+ 后台音频处理需要） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- 通知权限（Android 13+） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 保活权限：允许加入电池优化白名单（引导页"后台保活"） -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 修改音频设置 -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Shizuku 权限 -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <!-- 无障碍服务 -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />

    <!-- 读取外部存储（导入/导出预设） -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- 悬浮窗权限（Task 7: 悬浮球 Service 需要 SYSTEM_ALERT_WINDOW） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 震动权限（Task 9: 悬浮球长按触发时震动反馈） -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- ============================================================
         Application
         ============================================================ -->

    <application
        android:name=".MaidMicApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MaidMic"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaidMic"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <!-- ============================================================
             主 Activity
             ============================================================ -->
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true"
            android:theme="@style/Theme.MaidMic">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============================================================
             方案 B: Shizuku AAudio 服务
             通过 Shizuku API 提升权限
             ============================================================ -->
        <service
            android:name=".bridge.shizuku.ShizukuMicService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Shizuku Provider -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false" />

        <!-- FileProvider（Task 7: voicepacks 文件分享） -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ============================================================
             方案 C: 无障碍服务
             ============================================================ -->
        <service
            android:name=".bridge.accessibility.MaidMicAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <!-- 无障碍配置（在 xml/ 目录下） -->
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- ============================================================
             保活前台服务
             ============================================================ -->
        <service
            android:name=".MaidMicKeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- ============================================================
             悬浮球前台服务（Task 7: 仅骨架，View 挂载见 Task 8）
             默认关闭，需用户主动开启
             ============================================================ -->
        <service
            android:name=".floating.FloatingBallService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

    </application>

    <!-- 查询已安装应用（沙箱模式备用方案） -->
    <queries>
        <package android:name="com.tencent.mm" />
        <package android:name="com.netease.party" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

</manifest>
