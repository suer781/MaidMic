# MaidMic 开发状态 📋

> 最后更新：2026-08-07

## 当前状态：在干了 在干了

以下记录当前进度、核心瓶颈和已知槽点，方便恢复开发时快速接续。
我还没 push 过来

---

## 瓶颈：实时变声拦截需要 Root

**这是整个项目最大的卡点。**

### 问题本质

Android 的音频系统（AudioFlinger）在没有 root 权限时，应用无法直接拦截系统级音频流并注入处理后的音频。当前三种桥接方案都有各自的局限：

| 方案 | 权限 | 能否拦截系统音频 | 局限 |
|------|------|:---:|------|
| **Root AudioFlinger** | Magisk root | ✅ 能 | 需要 root，普通用户无法使用 |
| **Shizuku AAudio** | Shizuku | ❌ 不能 | 只能处理 App 自身的音频，无法作为系统虚拟麦克风 |
| **无障碍服务** | 无障碍权限 | ❌ 不能 | 最兼容但同样无法拦截其他 App 的音频 |

### 为什么这么难

- Android 从 10 开始收紧了 `AudioRecord` 的权限，非 root App 无法录制其他 App 的音频
- `AudioPlaybackCapture`（Android 10+）理论上可以录制其他 App 音频，但需要目标 App 明确声明 `allowPlaybackCapture=true`，且**不能作为麦克风输入**
- 虚拟麦克风的终极目标是：**任何 App 都能用 MaidMic 作为麦克风输入源**（游戏语音、直播、视频通话），但 Android 没有开放"注册为音频输入设备"的非 root 路径
- `MediaProjection`（屏幕录制 API）可以捕获音频，但它是**输出流**，不能反向注入到输入设备

### 正在探索的方向
直接掀翻！
不搞实时了（这里说的是单个手机不搞实时）
吃了几个亿token选择换掉实时条路
---

## 🔴 其他槽点

### 1. DSP 延迟不稳定（新版，没测以下是当前没有push的版本槽点）
- 简单处理管线（增益+EQ）约 5-10ms，可接受
- 完整处理管线（增益+压缩+低音+高音+混响+变调+共振峰+失真+回声，9 模块默认链）延迟可能超过 50ms
- 实时变声场景下 >50ms 就有明显感知，>100ms 基本不可用
- **需要优化**：JNI 层流水线并行化、buffer size 动态调整

### 2. 大幅变调后人声不自然
- 变调已从"简单重采样"升级为相位对齐拼接交叉淡化（PSOLA 类时域算法，pitch.c v2）：拼接点对齐基频周期整数倍 + 子样本细化 + 等功率淡化，听感比旧版干净
- 但变调本质仍会移动共振峰，Formant Shifting 补偿只是 Shelving 滤波近似，±8 半音以上的大幅变调依旧偏假
- **需要**：真正的共振峰保持变调（phase vocoder / 频谱处理）或更好的共振峰补偿

### 3. Auto-Tune 引擎已实现但无 UI 入口
- 引擎侧 AutoTune 模块已完成（ACF 自相关基频检测 + 音阶量化 + retune/speed 平滑），JNI `nativeSetAutoTune` 已暴露，默认管线未预置
- App 端没有任何界面调用它，用户目前用不到
- **需要**：在变声页接入 Auto-Tune 控制（音阶 / 修正强度 / 收敛速度）

### 4. UI 有待打磨
- 滑块交互不够精细，微调困难
- 预设切换时偶有爆音

---

## ✅ 已完成的里程碑

- [x] 双引擎架构（直通 / Echio 均衡）
- [x] Echio 引擎：增益、压缩、EQ、变调、混响、共振峰偏移、失真、回声（9 模块默认链）
- [x] 5 个变声预设（萝莉/大叔/机器人/原声/自定义）
- [x] 四 Tab 主界面（变声 / EQ / 音效库 / 设置）+ 悬浮球
- [x] 音效库 + 语音包（录音→变声→存包→外放）
- [x] 预录音变声链路（录音 → 实时变声 → 存包 → 外放）
- [x] Material 3 暗色主题
- [x] 前台保活 + 状态持久化

## 📅 未来可能实现

1. **优化 DSP 延迟** — 目标完整管线 <30ms
2. **Auto-Tune UI 接入** — 引擎 + JNI 已就绪（autotune.c），缺 App 端入口
3. **插件系统** — 暂停开发，以后继续（Plugin 接口、SO 加载、动态注册）
4. **Chorus** — module.h 已预留 ID 6，需实现；Noise Gate 引擎已完成，待接入默认链 + UI
5. **PC 协同（非实时方向）** — 手机采集 → PC 虚拟麦克风（已有 streaming 双设备流式传输雏形）

---

*本项目由 [我是真的会谢](https://github.com/suer781) 独立开发*
*B站：https://b23.tv/JvcdN4I | 抖音：https://v.douyin.com/cT8XUPBO*
